name: MCP CI

# T-517: у нового пакета обязан быть свой гейт. Без него код MCP-сервера попадал бы
# в main без единой проверки — K-08 требует, чтобы те же проверки проходили в CI.
on:
  push:
    branches: [ main ]
    paths: [ 'mcp/**', '.github/workflows/mcp-ci.yml' ]
  pull_request:
    paths: [ 'mcp/**', '.github/workflows/mcp-ci.yml' ]
  # T-303: см. пояснение в frontend-ci.yml — CVE появляются без изменений кода.
  schedule:
    - cron: '0 6 * * 1'

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: mcp

    steps:
      - uses: actions/checkout@v4

      - name: Read .nvmrc
        id: nvmrc
        run: echo "version=$(cat .nvmrc)" >> $GITHUB_OUTPUT

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ steps.nvmrc.outputs.version }}
          cache: npm
          cache-dependency-path: mcp/package-lock.json

      - name: Install dependencies
        run: npm ci

      # T-303: гейт, а не отчёт. Порог и обоснование — как во frontend-ci.yml.
      # На момент введения пакет имел 0 уязвимостей любого уровня.
      - name: Security audit
        run: npm audit --audit-level=high

      - name: Type check
        run: npm run lint

      - name: Test
        run: npm test

      - name: Build
        run: npm run build
