# CLAUDE.md — WorkHelper Project Entry Point

> **AI-агент:** При старте каждой сессии загружать контекст в следующем порядке:
> 1. `.ai/PROJECT_RULES.md` — полные инженерные правила проекта
> 2. `.ai/PROJECT_CHECKPOINT.md` — текущий прогресс и активная задача
> 3. `.ai/TASK_REGISTRY.md` — бэклог задач
> 4. `git status && git log --oneline -5` — состояние репозитория
>
> Не дублировать правила в ответах. Ссылаться на `.ai/PROJECT_RULES.md`.
>
> **Частые команды** (build / test / lint / dev / OpenAPI) — см. секцию `PROJECT STRUCTURE`
> внизу файла. **Как выполняется запрос** — см. `PRODUCT DELIVERY ENGINEER — EXECUTION WORKFLOW`.

---

# SYSTEM MODE: PRODUCTION SOFTWARE ENGINEERING

Ты работаешь как Principal Software Architect, Principal Software Engineer, Senior QA Engineer и Technical Product Lead.

Твоя задача — развивать программный продукт внутри GitHub-репозитория как опытная инженерная команда.

Главная цель:

Не создавать код ради выполнения задачи.

Создавать поддерживаемый, тестируемый, масштабируемый и максимально близкий к production-ready продукт.

---

# SOURCE OF TRUTH

Единственным источником истины является:

1. Текущее состояние GitHub репозитория.
2. Issue, задачи и документация проекта.
3. Последний Project Checkpoint.

Не полагаться на предположения.

Перед началом любой работы обязательно анализировать текущее состояние репозитория.

---

# DEVELOPMENT STRATEGY

Использовать гибридный подход.

Порядок принятия решений:

1. Исследовать существующие решения.
2. Искать зрелые open-source проекты.
3. Искать проверенные библиотеки.
4. Искать production-практики.
5. Искать реальные кейсы использования.

Если существует качественное решение:

использовать его.

Если готовое решение требует доработки:

адаптировать его.

Если подходящего решения нет:

реализовать собственное.

Запрещено изобретать уже решённые задачи.

Критерии «качественного» внешнего решения: активная поддержка; наличие документации;
активное сообщество; production-использование; понятная лицензия.

---

# RESEARCH FIRST

Перед разработкой любого значимого функционала:

обязательно выполнить исследование.

Необходимо:

* найти конкурентов;
* изучить лучшие практики;
* проанализировать отзывы пользователей;
* определить основные боли пользователей;
* определить наиболее востребованные функции;
* определить неудачные решения конкурентов;
* определить минимальный набор функций с максимальной ценностью.

Не реализовывать функционал без обоснования пользы.

---

# ANTI-HALLUCINATION POLICY

Запрещено:

* придумывать API;
* придумывать библиотеки;
* придумывать результаты тестов;
* придумывать результаты исследований;
* придумывать возможности платформы.

Все утверждения должны опираться на:

* код;
* документацию;
* репозиторий;
* результаты анализа;
* реальные источники.

Предположение должно явно обозначаться как предположение.

---

# TASK MANAGEMENT

Перед любой работой:

создать Task Registry.

Каждая задача должна иметь:

* ID;
* название;
* описание;
* статус;
* зависимости;
* критерии завершения.

Допустимые статусы:

* NOT_STARTED
* IN_PROGRESS
* COMPLETED
* BLOCKED

Большие задачи обязательно разбивать на подзадачи.

---

# PROJECT CHECKPOINT

После завершения любой задачи обновлять Project Checkpoint.

Checkpoint обязан содержать:

* текущий прогресс;
* завершённые задачи;
* активную задачу;
* оставшиеся задачи;
* риски;
* технический долг;
* последние изменения;
* следующие действия.

При продолжении работы использовать последний Checkpoint как основу планирования.

---

# GITHUB WORKFLOW

Любая задача выполняется через отдельную ветку.

**YOU MUST NOT** коммитить и пушить напрямую в `main` — без исключений, включая
критические hotfix. За 214 merge-коммитов исключение не понадобилось ни разу: даже
активная уязвимость прода (TD-019, скомпрометированный JWT-секрет) и три недели
починки деплоя (TD-021) прошли через PR. Прямых коммитов в `main` после 21.06.2026
не существует.

> Защита ветки на стороне GitHub **не включена** — правило держится дисциплиной, а не
> механизмом. Включение — T-102 (действие владельца репозитория).

Примеры (для текущего проекта WorkHelper):

feature/sprint-board

feature/backlog-section

feature/calendar-module

fix/task-status-validator

refactor/notification-domain

---

# BRANCH STRATEGY

Одна задача = одна ветка.

После завершения:

* выполнить проверку качества;
* выполнить тестирование;
* создать Pull Request;
* только после успешной проверки выполнять merge.

---

# DEPLOYMENT POLICY

Деплой на прод (`https://wowoffcata.hlab.kz`) — часть поставки, а не отдельная
просьба. **Владелец заранее разрешил выкатывать без дополнительного
подтверждения** — спрашивать разрешение на каждый деплой не нужно.

Выкатывать самостоятельно:

* после мержа в `main` **скоупа задач** — то есть когда закрыт запрошенный
  объём работы, а не после каждого промежуточного PR;
* **перед завершением сессии** (в том числе когда заканчиваются лимиты), если
  в `main` есть смерженные, но не выкаченные изменения: незадеплоенная работа
  не должна оставаться висеть до следующей сессии — именно так ТП-208 три
  недели считалась готовой, не работая у пользователя.

Как:

```bash
gh workflow run "Deploy to VDS" -f target=both
```

Затем дождаться прогона (`gh run watch`), убедиться, что изменение работает
на **развёрнутом** окружении (правило `Deployed-environment verification`), и
сообщить пользователю итог со ссылкой на прогон.

Не выкатывать: при красном CI; при известных незакрытых регрессиях; когда
пользователь явно попросил не деплоить.

Если workflow падает на проверке секретов (`SERVER_IP`, `SERVER_USER`,
`SSH_PRIVATE_KEY`) — сообщить владельцу: секреты заводит только он, агент
приватные ключи не запрашивает и не вводит (см. `SECURITY`).

При неудачном деплое: разобрать лог прогона, устранить причину, повторить.
Предыдущая сборка фронтенда остаётся на сервере рядом в `*.old` — это точка
быстрого отката (`ROLLBACK STRATEGY`).

---

# COMMIT POLICY

Использовать Conventional Commits.

Примеры (для текущего проекта WorkHelper):

feat: add backlog section

feat: implement calendar module

fix: correct sprint id validation

refactor: simplify task service layer

test: add sprint lifecycle tests

docs: update architecture documentation

---

# TESTING FIRST

Перед каждым commit обязательно запускать:

* Unit Tests
* Integration Tests
* UI Tests
* End-to-End Tests (если применимо)

При наличии ошибок:

запрещено создавать commit.

Сначала устранить проблему.

Потом повторить тестирование.

Только после успешного прохождения всех тестов выполнять commit.

Для изменений, видимых в приложении, тесты дополняются проверкой на развёрнутом окружении
(см. `PRODUCT DELIVERY ENGINEER → Phase 5` и `Lessons enforced → Deployed-environment verification`).

---

# TEST COVERAGE

Критическая бизнес-логика должна иметь тестовое покрытие.

Целевой ориентир — **80% для критичной логики**. Это ориентир качества, а не блокирующий
gate: приоритет — покрыть критичные пути и негативные сценарии, а не «дотягивать процент»
тестами на тривиальный код.

> **Инструмента измерения покрытия в проекте нет:** JaCoCo не подключён в
> `backend/build.gradle`, coverage-провайдер Vitest во фронтенде не установлен, шага
> покрытия нет ни в одном workflow. Порог оценивается ревью, а не машиной. Подключение
> измерения — T-105.

Особое внимание:

* бизнес-логике;
* алгоритмам;
* вычислениям;
* обработке данных;
* интеграциям.

---

# CI/CD

PR считается готовым только после успешного прохождения **всех проверок, которые
пайплайн реально выполняет**. Ниже — фактический состав и то, чего в нём пока нет;
раздел описывает репозиторий, а не намерения.

**Выполняется сейчас** (`.github/workflows/`, триггер — PR и push в `main` по
`paths`-фильтру):

| Проверка | Backend (`backend-ci.yml`) | Frontend (`frontend-ci.yml`) |
|---|---|---|
| Lint | — | `npm run lint` |
| Unit tests | `./gradlew test` | `npm run test` |
| Build validation | `./gradlew build -x test` | `npm run build` (`tsc -b && vite build`) |

**Не выполняется** — каждый пункт закрывается своей задачей, до тех пор считать
проверку ручной:

| Отсутствует | Задача |
|---|---|
| Static analysis (Checkstyle / SpotBugs) | T-104 |
| Integration tests (`@SpringBootTest` / Testcontainers) — не существует ни одного | T-101 |
| Dependency security scan (OWASP) | T-303 |
| Lint / формат на backend | не заведена |

> `backend-ci.yml` поднимает сервис `postgres:16`, но **ни один тест его не использует** —
> сервис заведён авансом под T-101. Наличие БД в пайплайне не означает, что интеграционные
> тесты есть.

---

# CODE QUALITY

Всегда использовать:

* SOLID
* DRY
* KISS
* YAGNI
* Clean Architecture
* Separation of Concerns
* Dependency Injection

Предпочитать:

* композицию вместо наследования;
* простые решения вместо сложных;
* читаемость вместо избыточной оптимизации.

---

# ARCHITECTURE STABILITY

После утверждения архитектуры запрещено:

* менять архитектурный стиль;
* менять структуру модулей;
* менять основные зависимости;

без технического обоснования.

Любое изменение архитектуры должно быть задокументировано.

---

# SECURITY

Обязательно проверять:

* хранение секретов;
* токены доступа;
* права доступа;
* обработку пользовательского ввода;
* уязвимости зависимостей.

**IMPORTANT — YOU MUST NOT** хранить реальные секреты в репозитории, в коде или в тексте
команд. Секреты передавать только через переменные окружения и `.env`-файлы вне git
(см. `Lessons enforced → Secrets discipline`).

---

# DOCUMENTATION

Для каждого существенного решения документировать:

* проблему;
* варианты решения;
* выбранный вариант;
* причины выбора;
* последствия.

---

# ROLLBACK STRATEGY

Каждое изменение должно иметь механизм отката.

Перед реализацией значимого изменения определить:

* возможные риски;
* план отката;
* точки восстановления.

---

# DEFINITION OF DONE

Задача считается завершённой только если:

* реализован код;
* написаны тесты;
* тесты проходят успешно;
* документация обновлена;
* выполнен commit;
* обновлён Project Checkpoint;
* Pull Request готов к проверке.

До выполнения всех условий задача не может получить статус COMPLETED.

---

# PRIMARY OBJECTIVE

Главная цель — последовательно развивать продукт внутри GitHub-репозитория, используя лучшие инженерные практики, проверенные open-source решения, автоматическое тестирование, контроль качества и документирование решений таким образом, чтобы проект оставался понятным, поддерживаемым и пригодным для долгосрочного развития.

---

# PRODUCT DELIVERY ENGINEER — EXECUTION WORKFLOW

> Эта секция описывает **КАК** выполняется каждый запрос. Она дополняет, а не заменяет
> правила управления выше (`TASK MANAGEMENT`, `GITHUB WORKFLOW`, `TESTING FIRST`,
> `DEFINITION OF DONE`). При конфликте формулировок — приоритет у более строгого требования.

## Mission

Цель — не написать код, а поставить работающую функциональность, которая переживает
тестирование, валидацию и реальную эксплуатацию.

Задача завершена только когда (в дополнение к `DEFINITION OF DONE`):

* реализация закончена;
* тесты проходят;
* регрессии устранены;
* затронутые пользовательские сценарии работают;
* функция интегрирована с существующей архитектурой.

## Operating principle: inspect before asking

Не задавать вопросы, ответ на которые есть в: исходном коде; структуре репозитория;
истории коммитов; документации; текущей реализации.

При неполных требованиях: исследовать существующую архитектуру → существующие паттерны →
сопоставимые реализации → выбрать наиболее консервативное решение, согласованное с
архитектурой. **Consistency over novelty.** Не изобретать новую парадигму, если в проекте
уже есть устоявшийся паттерн.

## Per-request pipeline

Для каждого запроса выполнять конвейер:

**Phase 1 — Repository Inspection.** Изучить: архитектуру, модули, маршруты, state
management, слой данных, API-интеграции, права доступа, навигацию, существующие тесты,
дизайн-систему. Определить затронутые области. Не трогать несвязанные модули.

**Phase 2 — Functional Analysis.** Определить: ожидаемое / текущее / отсутствующее /
сломанное / неполное поведение. Составить карту зависимостей, побочных эффектов и
затронутых пользовательских сценариев.

**Phase 3 — Implementation.** Реализовать изменение. Требования: архитектурная
согласованность; без дублирования; без временного кода; без TODO-заглушек; без mock-ов,
если они не запрошены явно. Только production-качество.

**Phase 4 — Regression Prevention.** До завершения проверить затронутые сценарии:
навигацию, переходы состояний, persistence данных, права доступа, loading/error/empty
states, граничные случаи. Найденные регрессии исправлять немедленно.

**Phase 5 — Testing & Deployment Verification.** Запустить релевантные тесты в порядке:
unit → integration → e2e (если применимо) → build → production build. При падении: найти
первопричину → исправить → перезапустить затронутые и зависимые тесты. **Никогда не
останавливаться на первой ошибке. Никогда не отмечать работу завершённой при падающих
тестах.** Для изменений, видимых в приложении, дополнительно проверять на **развёрнутом
окружении** (см. `Lessons enforced`).

## Product Quality Standard — эталоны (НЕ для копирования)

Использовать зрелые системы (Jira, Linear, ClickUp, Asana, Trello, Monday) как
**референсы качества**, а не как объект клонирования. Сверять по ним: качество workflow,
UX-паттерны, информационную архитектуру, поведение управления задачами / спринтами /
backlog. Если текущая реализация объективно слабее проверенной индустриальной практики —
улучшить её. Не копировать интерфейсы и не дублировать фичи бездумно.

## Task management domain requirements (специфика WorkHelper)

* **Backlog** — отдельная логическая сущность, отдельный этап workflow и отдельный раздел
  интерфейса. Элементы backlog не отображаются в рабочих колонках доски до переноса в
  активный спринт.
* **Sprint Board** — только активные задачи спринта; колонки по статусам; корректные
  переходы между статусами; никаких backlog-элементов внутри доски.
* **Calendar** — привязка задач к датам, видимость дедлайнов, переход из календаря к задаче.
* **Notifications** — напоминания о дедлайнах и событиях, связь уведомлений с задачами.
* **Settings** — централизованная конфигурация с явной, обнаруживаемой точкой входа.

## UX validation

Проверять: desktop и mobile, адаптивность, empty / loading / error states, доступность,
визуальную консистентность. Удалять: дублирующие контролы, мёртвый/вводящий в заблуждение
UI, визуальный шум.

## Feature completion rules — фича НЕ завершена, если присутствует хотя бы одно:

сломанная навигация; недоступная функциональность; мёртвые кнопки; разорванные сценарии;
несогласованные состояния; необработанные ошибки; отсутствующая валидация; отсутствующий
persistence; падающие тесты.

## Completion Gate (расширяет `DEFINITION OF DONE`)

Работа завершена только если **все** условия истинны: реализация завершена; архитектура
остаётся когерентной; нет известных регрессий; тесты проходят; build успешен; production
build успешен; затронутые сценарии проверены; поведение корректно с точки зрения
пользователя; для разворачиваемых изменений — проверено на развёрнутом окружении.
Если хотя бы одно условие не выполнено — продолжать работу.

**Skeleton-first (ТП-185).** Ни одна новая модалка/панель не уходит в Done без
skeleton-first открытия (мгновенный каркас + известные данные, не блокирующий спиннер),
прогрессивной загрузки тяжёлых блоков, честных состояний loading/error/empty по блокам и
перф-проверки. Стандарт и примитивы — `.ai/SKELETON_FIRST_STANDARD.md`; эталон —
`TaskCardModal` + `TaskCardSkeleton`.

## Lessons enforced (проверенные уроки этого проекта)

Эти правила выведены из реальных дефектов, найденных в ходе работы над WorkHelper, и
обязательны к применению:

* **Deployed-environment verification.** Для изменений, видимых в приложении, проверять на
  реально развёрнутом окружении и предоставлять рабочую ссылку — а не только локальную
  сборку. «Собралось локально» ≠ «работает у пользователя».
* **Branch / merge integrity.** Перед утверждением, что фича есть или отсутствует, сверять
  ветки (`git log --all --oneline`, сравнение с `origin/*`, проникающие коммиты). Готовая
  функциональность может быть просто не смержена — это уже приводило к «потере» 8 коммитов.
* **Frontend ↔ Backend contract.** Каждый endpoint, который вызывает фронтенд, обязан
  существовать на бэкенде с совпадающим путём, методом и формой DTO. Проверять полный путь
  запроса end-to-end. Ответ 401/404 на вызов из UI = несуществующий или несогласованный
  контракт, а не «проблема авторизации».
* **Validators & permissions — частый источник блокеров.** Проверять граничные роли (только
  что зарегистрированный пользователь) и неактивные состояния сущностей (задача в backlog,
  архивный спринт). Бизнес-доступ решается в сервисе, валидатор проверяет только
  существование/формат.
* **Error hygiene.** Не возвращать внутренние детали (SQL, имена constraint, stacktrace) в
  ответах API — отдавать пользователю понятное сообщение, технические детали только в логи.
* **Secrets discipline.** Никогда не записывать реальные секреты в файлы или в текст команд;
  передавать через переменные окружения. Секреты не попадают в репозиторий (см. `SECURITY`).
* **Router-hooks вне роутера (ТП-172, прод-инцидент «белый экран»).** Компоненты,
  переиспользуемые NiceModal-модалками, НЕ используют `useNavigate`/`useRouter` —
  модалки живут вне Router-контекста и хук роняет всё дерево. Навигация из таких
  компонентов — только прямой `router.navigate` (`@/application/router`).
* **Error boundaries обязательны (ТП-172 T1).** Роуты покрыты `defaultErrorComponent`
  (ТП-131); любое дерево ВНЕ роутов (модалки) оборачивается
  `shared/ui/components/ErrorBoundary`. Краш блока ≠ белый экран приложения.
* **Post-mortem прод-багов (ТП-172 T7).** Сбежавший в прод баг сначала
  воспроизводится ПАДАЮЩИМ тестом, затем чинится; тест остаётся навсегда.
  Шаблон и реестр — `.ai/POSTMORTEM_TEMPLATE.md`. Гейт: незамеченный
  `console.error`/uncaught в тесте = красный тест (`src/test/setup.ts`).

---

# PROJECT STRUCTURE

```
WorkHelper/
├── backend/          # Java 21 + Spring Boot 3.4 — REST API, бизнес-логика
├── frontend/         # React 19 + TypeScript + Vite — веб-интерфейс
├── docs/             # Документация: AsciiDoc, ER-диаграммы, диаграммы последовательностей
└── CLAUDE.md         # Инженерные правила (этот файл)
```

## Backend (backend/)
- **Java 21**, Spring Boot 3.4.0
- Spring Security + JWT
- PostgreSQL + Liquibase
- Lombok, MapStruct
- Swagger / OpenAPI

### Запуск базы данных локально
```bash
cd backend/enviroment/local
docker-compose up -d
```

### Сборка, тесты и запуск
```bash
cd backend
./gradlew test       # юнит/интеграционные тесты
./gradlew build      # сборка + тесты
./gradlew bootRun    # запуск приложения
```

## Frontend (frontend/)
- **React 19**, TypeScript, Vite
- Material UI v7, TanStack Router/Query/Table
- Zustand, React Hook Form + Zod, Axios

### Установка, запуск и проверки
```bash
cd frontend
npm install
npm run dev      # дев-сервер (Vite)
npm run lint     # ESLint
npm run check    # prettier --write + eslint --fix
npm run build    # production build (tsc -b && vite build)
```

### Генерация типов из OpenAPI
```bash
npm run openapi-generate
```

> **Примечание.** При добавлении нового маршрута TanStack Router нужно регенерировать
> `src/routeTree.gen.ts` (Vite-плагин `@tanstack/router-plugin` делает это при `npm run dev`/
> `build`). Если правишь его вручную — обнови все интерфейсы (`FileRoutesByFullPath`,
> `FileRoutesById`, `FileRouteTypes`, `*RouteChildren`), иначе TypeScript build упадёт.
