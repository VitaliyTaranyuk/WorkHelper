/**
 * Якоря для подсветки в обучении (ТП-118). Один источник имён `data-tour`, чтобы
 * компонент, который ставит атрибут, и шаг тура, который на него ссылается, не
 * разъезжались. Пока нужен один стабильный якорь — кнопка микрофона: практика
 * всегда просит «нажмите микрофон и скажите …».
 */
export const TOUR_ANCHORS = {
  voiceMic: 'voice-mic',
} as const

export const voiceMicSelector = `[data-tour="${TOUR_ANCHORS.voiceMic}"]`
