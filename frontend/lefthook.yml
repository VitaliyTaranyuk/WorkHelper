# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
#
pre-push:
  parallel: true
  jobs:
    - name: build
      tags:
        - valid
      run: npm run build

pre-commit:
  parallel: true
  jobs:
    - name: lint
      tags:
        - valid
      run: npm run lint
