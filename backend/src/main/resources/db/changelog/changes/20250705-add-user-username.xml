<?xml version="1.1" encoding="UTF-8" standalone="no"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.1.xsd">

    <!-- Additive: username (unique, nullable) + display_name for profiles & @mentions. -->
    <changeSet id="20250705-add-user-username-columns" author="claude">
        <preConditions onFail="MARK_RAN">
            <not><columnExists tableName="users" columnName="username"/></not>
        </preConditions>
        <addColumn tableName="users">
            <column name="username" type="varchar(64)"/>
            <column name="display_name" type="varchar(255)"/>
        </addColumn>
    </changeSet>

    <changeSet id="20250705-user-username-unique" author="claude">
        <addUniqueConstraint tableName="users" columnNames="username" constraintName="uk_users_username"/>
    </changeSet>

    <!-- Keep 3 named test users (@admin/@ivanov/@petrov); deactivate the rest. -->
    <changeSet id="20250705-seed-usernames" author="claude">
        <sql>
            UPDATE users SET username='admin',  display_name='Админ'      WHERE email='test1@mail.ru' AND username IS NULL;
            UPDATE users SET username='ivanov', display_name='Иван Иванов' WHERE email='test2@mail.ru' AND username IS NULL;
            UPDATE users SET username='petrov', display_name='Пётр Петров' WHERE email='test3@mail.ru' AND username IS NULL;
            UPDATE users SET is_active=false WHERE email IN ('test4@mail.ru','test5@mail.ru','test6@mail.ru');
        </sql>
        <rollback/>
    </changeSet>

</databaseChangeLog>
