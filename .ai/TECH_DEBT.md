# TECH_DEBT.md

> Реестр технического долга проекта WorkHelper.
> Обновлять при обнаружении нового долга или устранении существующего.

---

## Активный технический долг

| ID | Описание | Причина | Риск | Статус |
|----|----------|---------|------|--------|
| TD-001 | Credentials DB в `application-local.yml` в репозитории | Быстрая настройка локального окружения | HIGH: утечка паролей если станет публичным | OPEN |
| TD-002 | Отсутствие Integration Tests (только Unit Tests) | Начальный этап покрытия | MEDIUM: нет проверки реального DB-взаимодействия | OPEN |
| TD-003 | Защита ветки `main` на GitHub не включена (проверено 2026-07-28: API отвечает `Branch not protected`) — прямой push и merge при красном CI технически возможны | Требует доступа к настройкам репозитория, которого у агента нет | MEDIUM: правило «только через PR» держится дисциплиной, а не механизмом. **Фактически соблюдается:** 214 merge-коммитов, прямых коммитов в `main` после 21.06.2026 нет ни одного. Устраняется T-102 — **действие владельца** | OPEN |
| TD-004 | `ddl-auto: none` только в local/prod, но в CI используется H2/Postgres без Liquibase init | Неполная CI конфигурация | MEDIUM: тесты могут расходиться с реальной схемой | OPEN |
| TD-005 | Подсистема спринтов (entity/controller/service/board) не удалена | Решение «medium» + аддитивные миграции; видение Kanban спринтов не предполагает | MEDIUM: лишний код/таблицы, спрятать в UI как follow-up | **ЗАКРЫТ ПЕРЕОЦЕНКОЙ 2026-08-05** (T-519, **ADR-028**, решение владельца). Это больше не долг: спринты — не «лишний код, который надо снести», а рабочая половина переключателя `project.board_mode`. Проект в режиме `KANBAN` спринтами не пользуется и не показывает их контролов, в режиме `SPRINT` — пользуется; переключение обратимо. Удаление отвергнуто по измеренной цене: `task_model.sprint_id` — `NOT NULL` с FK `ON DELETE RESTRICT`, разрушающая миграция при недоступном откате Liquibase, а **бэклог реализован КАК спринт** — его механизм пришлось бы строить заново |
| TD-006 | Сущности Comment / Link / TaskHistory не удалены | «Минимальный набор полей» не включает их, но дроп необратим на общей БД | LOW: неиспользуемые таблицы | OPEN |
| TD-007 | Поля задачи priority/assignee/taskType/estimation сохранены | Аддитивный подход; используются текущим UI/маппингом | LOW: избыточные поля относительно минимального видения | OPEN |
| TD-008 | Статусы REVIEW/CANCELED остаются в дефолтных колонках | Не дропаем из БД; видение — 4 колонки | LOW: доска показывает 6 колонок вместо 4 | OPEN |
| TD-009 | Фильтры assignee/creator (dropdown) сохранены наряду с My Tasks | Полный снос dropdown ломает типы/виджет при strict TS | LOW: «только My Tasks» выполнено частично | OPEN |
| TD-010 | Удаление проекта — мягкое (status=DELETED), не физическое | Data safety + каскад FK на tasks/sprints | LOW: записи остаются в БД | OPEN |
| TD-011 | `WORKTECH_API_BASE_URL` зашит в коде (нет env-переменной) | Историческое; локально требуется ручная правка/прокси; фолбэк работает, переопределяется `VITE_API_BASE_URL` | LOW: неудобство локальной разработки | OPEN |
| TD-012 | «Завершающая» колонка доски определяется позиционно (max priority среди видимых), а не явным флагом в модели | Минимальный фикс BUG-033 без миграции | LOW: перестановка колонок меняет семантику завершения; правильное решение — флаг `completedStatus` в TaskStatus (аддитивная миграция) | OPEN |
| TD-013 | Сайдбар шлёт `sprint-info` по каждому проекту и получает 404 при отсутствии активного спринта (шум в логах/сети) | Исторический контракт эндпоинта | LOW: корректнее 200 с пустым телом | OPEN |
| TD-014 | e2e-тесты фронтенда (Playwright) отсутствуют — сквозные UI-регрессии ловятся только ручным прогоном. Vitest поднят в ТП-91 (voice-модуль покрыт, прогон в CI); широкое покрытие компонентов ещё не сделано | Не было изначально | MEDIUM: см. T-203; расширять покрытие Vitest по мере доработок | OPEN |
| TD-015 | `NiceModal.Provider` смонтирован вне `RouterProvider`: router-хуки и `<Link>` в любых модалках падают (белый экран, BUG ТП-39); плюс нет глобального ErrorBoundary — любой краш рендера обваливает всё приложение | Историческая структура main.tsx | MEDIUM: правильное решение — `createRouter({ Wrap })` c NiceModal внутри router + ErrorBoundary на корне | **УСТРАНЁН ТП-131, запись устарела; исправлена 2026-08-07.** Долг числился `OPEN` и описывал механизм, которого в коде уже нет (**K-40**) — найдено при сверке остатка работ. Измерено: `application/router.tsx:33` монтирует `NiceModal.Provider` через опцию `Wrap`, то есть **внутри** дерева роутера (`main.tsx:41-43` содержит только пояснение и `Toaster`), поэтому router-хуки и `<Link>` в модалках работают; `application/router.tsx:23` задаёт `defaultErrorComponent: RouteErrorFallback` — восстановимый экран вместо падения приложения при краше рендера маршрута. Оба пункта записанного решения выполнены буквально. **Граница:** `defaultErrorComponent` покрывает маршруты, а дерево вне роутов закрывается отдельным `shared/ui/components/ErrorBoundary.tsx` — он применён к `TaskCardModal:135`; «одного boundary на всё дерево» нет и не планировалось. **R-02 остаётся действующим правилом** независимо от этого долга: компоненты для NiceModal-модалок по-прежнему не используют `useNavigate`/`useRouter` |
| TD-016 | Голос: Web Speech API не поддерживается в Firefox и зависит от онлайн‑сервиса распознавания браузера (Chrome шлёт аудио Google; при блокировке — ошибка `network`) | Стандартный браузерный ASR без ключей/серверов (ТП‑88) | LOW: нужен фиче‑детект и graceful‑fallback к вводу с клавиатуры; заменяемо через `SpeechProvider` (ADR‑009) | OPEN |
| TD-017 | Голос: командный режим полноценно работает только с LLM (I2/ТП‑96); без ключа доступен лишь ограниченный набор через RuleBased | Ключ AI (ТП‑10) — внешняя зависимость; сознательно отложен | MEDIUM: архитектура допускает подключение LLM позже без переработки (шов `IntentResolver`, авто‑фолбэк); не блокирует Веху 1. ТП‑208 завёл DeepSeek-ключ и backend-прокси, но ТОЛЬКО для качества текста (`/voice/enhance-text`) — `RemoteResolver` для командного намерения (I2) этим ключом не реализован, остаётся отдельной задачей | OPEN |
| TD-018 | Голос: политика приватности аудио/транскриптов не формализована | Этап проектирования | LOW: ASR локально по умолчанию, аудио не хранить; при облачном провайдере (I2) — зафиксировать retention | OPEN |
| TD-019 | JWT-секрет (`spring.app.jwtSecret`) был захардкожен в `application.yml` (`mySecretKey123...`), общий для всех профилей включая prod — виден любому: **репозиторий публичный**, а на проде значение ничем не переопределялось | Историческое; секрет никогда не выносился в окружение | HIGH: обладая ключом, можно было подделать JWT и войти под любым пользователем прода, включая администратора | **УСТРАНЁН 2026-07-27** (PR #186): `jwtSecret` берётся только из `${JWT_SECRET}`; `JwtUtils#initSecret` — fail-fast на старте (прежнее значение отвергается в ЛЮБОМ профиле, пустой секрет вне local/test останавливает запуск, короткий/не-base64 тоже; в local/test генерируется одноразовый). На VDS сгенерирован новый секрет (`openssl rand -base64 48`), задеплоено и проверено: токен, подписанный утёкшим ключом, отвергается с 401, повторный логин и новые токены работают. 8 регресс-тестов |
| TD-020 | `backend/enviroment/local/docker-compose.yml` использует нефиксированный тег `image: postgres:latest` | Исторически написано до выхода Postgres 18 | MEDIUM: `postgres:latest` теперь резолвится в 18+, несовместимую с текущим макетом volume (`postgres_data:/var/lib/postgresql/data`) — локальный docker-compose не поднимается на новых машинах без ручного вмешательства (обнаружено при live-верификации ТП-195: пришлось поднимать `postgres:16` контейнером в обход compose-файла) | **УСТРАНЁН 2026-07-27**: тег зафиксирован на `postgres:16` — той версии, на которой окружение реально проверялось (в т.ч. при верификации ТП-212) |
| TD-021 | Деплой бэкенда не автоматизирован и не описан: README обещает `.github/workflows/backend-deploy.yml`, `frontend-deploy.yml` и K3s-кластер, но в репозитории только `backend-ci.yml`/`frontend-ci.yml`, а деплой-скриптов не было ни разу за всю историю (`git log --all --diff-filter=A -- "*deploy*"`) | Документация опережала реализацию | HIGH: смерженный код может неограниченно долго не доходить до пользователя, и это НЕ видно из репозитория. Обнаружено при аудите ТП-212: ТП-208 (`POST /voice/enhance-text`) смержен в `main` 2026-07-26, но на `wowoffcata.hlab.kz` эндпоинт отвечает 401 так же, как несуществующий путь, — то есть на VDS работает образ, собранный до ТП-208. Фикс: workflow ручного деплоя по SSH | **УСТРАНЁН 2026-07-28**: workflow (`.github/workflows/deploy.yml`) впервые доехал до прода — [прогон #30334771283](https://github.com/VitaliyTaranyuk/WorkHelper/actions/runs/30334771283), фикс списка задач выложен и проверен вживую. Владелец завёл секреты; попутно выяснилось, что вход по ключу для него недостижим (многострочный ключ портится при заливке через PowerShell-пайп), поэтому поддержан вход по паролю (PR #196), добавлена обрезка перевода строки в пароле (#197) и `keyboard-interactive` (#198) — без последнего сервер с PAM отвергает пароль, симптом неотличим от неверного. Ключ остаётся приоритетным способом, возврат к нему не требует правок workflow |
| TD-026 | Учётные данные администраторов в публичном репозитории: `AdminUsersBootstrap.DEFAULT_PASSWORD = "password12345"` (создавал `vt@`/`mk@`/`ek@mail.ru` на каждом старте) и liquibase-миграция `20250604-add-user-roles.xml` с общим bcrypt-хешем того же пароля для `test1..test6@mail.ru` | Историческое; удобство первого запуска | HIGH: логин и пароль администраторов боевого сервера знал любой, кто открыл GitHub — вход без всякого взлома. Найдено 2026-07-27 при сплошной проверке секретов после TD-019 | **ЧАСТИЧНО УСТРАНЁН 2026-07-27**: источник закрыт — пароль вынесен в `BOOTSTRAP_ADMIN_PASSWORD`, пустое значение означает «не создавать учётки вовсе» (6 регресс-тестов). **Требуется действие владельца:** сменить пароли уже существующих `vt@`/`mk@`/`ek@mail.ru` и решить судьбу `test1..test6@mail.ru` (отключить или сменить пароли) — сделать это за пользователей нельзя, они могут быть рабочими. Миграцию править нельзя (checksum Liquibase), нужна новая при необходимости |
| TD-023 | `backend/Dockerfile` копировал готовый `app.jar` (`COPY app.jar app.jar`), а не собирал исходники; `app.jar` в git не хранится | Историческая схема «собери где-нибудь и положи рядом» | HIGH: `git pull` + `docker compose up --build` на сервере НЕ обновляли приложение — образ переупаковывал старый jar, и это выглядело как успешный деплой. Именно так ТП-208 «деплоилась» три недели (проверено 2026-07-27: после pull и пересборки эндпоинт оставался 401, помогла только ручная сборка jar и заливка) | **УСТРАНЁН 2026-07-27**: многоэтапный Dockerfile (`gradle:8.10-jdk21` → `amazoncorretto:21`), jar собирается внутри образа. Опасение «на VDS не хватит ресурсов» проверено на живом сервере (1 CPU, 1.8 ГБ RAM): сборка проходит при `-Xmx512m` и отключённом демоне Gradle, образ содержит актуальные классы. Добавлен `.dockerignore`, чтобы старый `app.jar` из контекста больше ни на что не влиял |
| TD-024 | Сборка фронтенда требовала `VITE_API_BASE_URL`: без переменной бандл уходил на захардкоженный дефолт `http://91.211.249.37/test` — то есть в ЧУЖОЙ backend | Деплой не автоматизирован (см. TD-021) | MEDIUM: молчаливая поломка — приложение собиралось и деплоилось без единой ошибки, но работало не с тем сервером | **УСТРАНЁН 2026-07-27**: дефолт стал пустым — запросы идут относительными путями к тому же origin, откуда загружен фронтенд (их проксирует nginx). Забытая переменная больше не может увести бандл на чужой хост: в худшем случае фронт обратится сам к себе. `buildMeetWsUrl` при пустом базовом адресе берёт `window.location.origin` (WebSocket требует абсолютный URL), покрыто тестом. Побочно вскрылся хрупкий тест `createTaskCommand`: он ходил в сеть, и «спасал» его лишь недоступный дефолтный хост — теперь шов `enhanceTaskDraftSafe` замокан |
| TD-025 | GlitchTip на проде не принимал события: в консоли `Invalid Sentry Dsn: https://…@wowoffcata.hlab.kz/1` | В DSN подставлялся публичный ключ в том виде, в каком его показывает GlitchTip — UUID С ДЕФИСАМИ | MEDIUM: мониторинг клиентских ошибок молча не работал — ошибки прода не собирались (дефект существовал с 7 июля, к ТП-212 отношения не имеет) | **УСТРАНЁН 2026-07-27**: причина — `DSN_REGEX` в `@sentry/core` допускает в publicKey только `\w+`, дефисы делают DSN невалидным, и SDK отключается целиком. Ключ в DSN пишется без дефисов (`f6cee9c358cd…`), GlitchTip такой принимает (проверено: `store` → 200). Фронтенд пересобран и выложен; проверено сквозно — тестовая ошибка из браузера долетела до GlitchTip (issue «ТП-212 проверка мониторинга»). Формат зафиксирован в README |
| TD-029 | Healthcheck backend не отличал «поднялся» от «работает»: `docker-compose.vds.yml:13` — `["CMD", "curl", "-s", "http://localhost:8080/work-task/api/v1/auth/login"]`. Флаг **`-f` отсутствовал**, поэтому `curl` завершался успешно на любом HTTP-ответе, включая 500; вдобавок `/auth/login` — POST-эндпоинт, GET на него отдаёт 405. Контейнер становился `healthy`, если Spring отвечал хоть чем-нибудь | Написано как проверка доступности порта, а используется как гейт деплоя | MEDIUM: `deploy.yml` объявляет релиз успешным по этому статусу — сломанное приложение (например, упавшая Liquibase-миграция) прошло бы гейт. Класс «молчаливого отказа» (W-06), из-за которого TD-021 три недели считалась закрытой. Найдено 2026-07-28 при T-502 | **УСТРАНЁН 2026-07-29** (PR #216, [прогон #30435932759](https://github.com/VitaliyTaranyuk/WorkHelper/actions/runs/30435932759): контейнер прошёл `starting → healthy`, `/health` на публичном адресе отдаёт `{"status":"UP","database":"UP"}`): добавлен `GET /work-task/api/v1/health` (`HealthController` + `HealthService`, permitAll), который реально ходит в пул соединений и отвечает **503** при недоступной БД; healthcheck переведён на `curl -sf … \|\| exit 1` — как у glitchtip. Побочно найден и закрыт **более опасный дефект того же гейта**: шаг ожидания в `deploy.yml` сравнивал статус по подстроке (`case "$status" in *healthy*`), а строка `Up 2 minutes (unhealthy)` эту подстроку содержит — гейт принимал НЕЗДОРОВЫЙ контейнер за здоровый, то есть один `-f` ничего бы не изменил (измерено: `case` совпадает). Статус читается точным значением через `docker inspect`. Связь «конфигурация деплоя ↔ код» закреплена тестом `DeployGateContractTest`; `backend-ci.yml` запускается и на правку `docker-compose.vds.yml`/`deploy.yml` |
| TD-030 | Деплой не может доказать, что на сервере запущен образ **из нового кода**: у backend нет отпечатка версии. `build.gradle` вычисляет `appVersion = 0.0.1-<git sha>`, но внутри docker-образа `.git` отсутствует (в контекст копируются только `build.gradle`, `settings.gradle`, `gradle/`, `src/`), поэтому `git rev-parse` там возвращает пустую строку и версия вырождается в `0.0.1-`. Гейт проверяет, что на сервере нужный коммит и что приложение работает, но не что работает **этот** коммит | Версия никогда не пробрасывалась в сборку образа | MEDIUM: ровно этот класс отказа три недели выдавал ТП-208 за развёрнутую (TD-023) — для фронтенда он теперь закрыт сверкой бандла, для backend нет. Найдено 2026-07-29 при закрытии TD-029. Фикс: передавать SHA в `docker build --build-arg`, отдавать его в `/health` и сверять в `deploy.yml` | **УСТРАНЁН 2026-07-30** (T-305): `APP_VERSION` приезжает build-аргументом и объявлен только в рантайм-стадии Dockerfile (в стадии сборки он сбросил бы кэш слоёв Gradle на VDS с одним CPU); `/health` отдаёт поле `version`; шаг деплоя сверяет его с коммитом **на сервере**, а не с `github.sha` — гейт `merge-base` намеренно допускает, что сервер ушёл вперёд, и строгое равенство давало бы ложные падения |
| TD-028 | `GET /users/picker` не ограничен проектом: `UserService.pickerSearch` фильтрует по активности, `isSystem` и наличию username, но берёт `userRepository.getUsers()` — **всех пользователей системы**. `UserPickerDto` при этом отдаёт `email`. Комментарий над эндпоинтом («доступен всем участникам проекта») читается как scope, которого нет | Написано, когда проект был один — разницы не было видно | MEDIUM: любой участник любого проекта видит имена и **email всех пользователей системы** — расходится с K-36 (ПДн: минимизировать, ограничивать по ролям). Плюс мёртвый выбор в UI: назначить не-участника нельзя, `findAndCheckActiveUser` отвергнет, но он есть в списке (K-32). Найдено 2026-07-28 аудитом T-500 | **УСТРАНЁН 2026-07-29**: `/users/picker` получил обязательный `projectId` и отдаёт **только участников проекта**; членство вызывающего проверяется `CheckerUtil` — тем же механизмом, что и в остальных запросах. Контр-анализ показал, что «просто отфильтровать по проекту» сломало бы приглашение: `InviteUsersModal` использовал тот же эндпоинт, а приглашать нужно ровно тех, кого в проекте ещё НЕТ. Пути разделены: `GET /users/lookup` ищет по **точному** username или email (роли как у `add-users`), исключает уже добавленных и **не отдаёт email**. Подстроковый поиск по всей базе не спасала бы и роль: владельцем проекта становится любой, кто создал проект (`POST /projects/create`, PROJECT_MEMBER+), то есть каталог оставался бы перечисляемым. Побочно закрыта вторая половина того же дефекта: `createMentionNotifications` уведомлял по `@username` ЛЮБОГО пользователя, включая постороннего, — тот получал уведомление с кодом чужой задачи и неоткрываемой ссылкой |
| TD-034 | В прод-конфиге nginx **три идентичные** ingest-локации GlitchTip: `location ~ ^/api/[0-9]+/(envelope\|store\|security)/` в строках 29, 43 и 51 одного server-блока (снято 2026-07-30 диагностикой `apply-nginx-cache.sh`). Текущая версия `apply-nginx-glitchtip.sh` идемпотентна — проверяет маркер и пропускает файл, — значит дубли появились до появления этой проверки | Ранние версии скрипта применялись вручную и без маркера | LOW: функционально безвредно (nginx берёт первое совпадение по regex), но конфиг расходится с эталоном `infra/nginx-vds.conf` и запутывает разбор — на них уже ушло время при T-307-разборе. Устраняется **T-307** | OPEN |
| TD-033 | `infra/NGINX_WS_RUNBOOK.md` утверждает, что `apply-nginx-ws.sh` применяется «из пайплайна деплоя (`deploy-main.mjs`, шаг „Apply nginx WS location“) после `git pull` на VDS», и что «ручные правки прод-nginx не используются». Ни файла `deploy-main.mjs`, ни такого шага в репозитории нет: `git ls-files` не знает о нём, в `.github/workflows/` вызова скрипта нет. **Дополнено 2026-08-03 (A1 задачи T-307): мест не одно, а три** — то же самое утверждают заголовки самих скриптов, `infra/apply-nginx-ws.sh:2-4` и `infra/glitchtip/apply-nginx-glitchtip.sh:3`; второй скрипт в описании долга не упоминался вовсе, и он тоже не вызывается ниоткуда | Рунбук написан под пайплайн, который к моменту автоматизации деплоя (TD-021) собрали иначе | MEDIUM: документ описывает механизм, которого не существует (**K-40**), а WS-локация на проде держится ручным запуском — при пересоздании сервера её просто забудут. Найдено 2026-07-30 при заведении кэш-заголовков. Фикс: вызвать `apply-nginx-ws.sh` шагом `deploy.yml` рядом с `apply-nginx-cache.sh` и привести к реальности **все три** места. **Осторожно (A2 2026-08-03):** буквальный вызов «как есть» свяжет деплой с живостью мониторинга и WS — обе ветки «конфиг не менялся + post-check плохой» завершаются `exit 1` (`apply-nginx-glitchtip.sh:201-203`, `apply-nginx-ws.sh:130-132`). Развилка вынесена в описание T-307 | OPEN |
| TD-032 | Массовые операции (T-152) не выведены в UI: `POST /tasks/bulk/archive`, `/bulk/delete`, `/bulk/move-status`, `/bulk/move-project` существуют и покрыты тестами, но во фронтенде нет ни одного вызова (`grep bulk` по `frontend/src` — пусто). **Уточнение 2026-08-03 (A1 задачи T-309):** эндпоинтов **пять**, а не четыре — есть ещё `/bulk/move-sprint`, и он **уже доступен пользователю** через обёртку `PUT /tasks/update-sprint` (`TaskController:360`), которую вызывает `FinishSprintModal`. Недоступны были четыре из пяти | Массовые действия требуют множественного выбора задач (чекбоксы + панель действий), которого в списках нет | LOW: недоступная функциональность (**K-32**), но обходной путь есть — те же операции доступны по одной задаче. Найдено 2026-07-30 при T-157 | **ЗАКРЫТ 2026-08-03** (T-309, решение владельца — вариант «минимальный обратимый срез»): в «Списке задач» появились чекбоксы, выбор диапазоном по `Shift`, панель действий снизу («Выбрано: N», `Esc` снимает) и три операции — «В архив», «Статус…», «В спринт…». `/bulk/delete` и `/bulk/move-project` **осознанно не выведены**: удаление необратимо, а перенос между проектами перевыдаёт код задачи (`TaskService:530`) и безвозвратно ломает старые ссылки и упоминания. Остаток долга сведён к этим двум операциям и переоценён как осознанное решение, а не пробел |
| TD-031 | `useProjectData` падает на проекте без колонок: завершающий статус вычисляется `reduce(...)` **без начального значения** (`features/project/query/useProjectData.ts`), и на пустом массиве это `TypeError`, а не пустой результат. Экран проекта обваливается в ErrorBoundary целиком | Колонки заводятся вместе с проектом (`createDefaultStatuses`), поэтому случай считался невозможным | LOW: вскрылось на фикстуре в тестах T-518 | **УСТРАНЁН 2026-08-03** (T-308): `resolveStatus` стал `undefined` на пустом списке вместо исключения. Контр-анализ поправил запись долга в двух местах: (1) инвариант держит **не** создание колонок, а `TaskStatusService.deleteStatus` — он отвергает удаление `defaultTaskStatus` и `isSystemStatus`, поэтому шесть системных колонок неудалимы даже при существующем эндпоинте удаления колонки; (2) «недостижимо штатным путём» верно только для БД: `mapProjectDtoToProjectInfo` превращает **отсутствующее** поле `statuses` в `[]`, то есть достаточно дрейфа контракта (**W-08**). Выдуманный завершающий статус отвергнут как молчаливый отказ (**W-06**) — он позволил бы «завершить» задачу в несуществующую колонку. Покрыто 3 тестами, из которых 2 падают без фикса (проверено откатом) |
| TD-027 | Локальный quality gate не автоматизирован: `frontend/lefthook.yml` объявляет `pre-commit → npm run lint` и `pre-push → npm run build`, но эти хуки **не установлены**. В `.git/hooks` лежит только `prepare-commit-msg`, которого в конфиге нет вовсе, а сам lefthook недоступен (`Can't find lefthook in PATH` при коммите); `prepare`/`postinstall`-скрипта, который выполнял бы `lefthook install`, в `package.json` нет | Хуки ставятся вручную, шаг не автоматизирован и со временем потерялся | MEDIUM: `PROJECT_RULES §QUALITY GATE` требует линт и сборку перед коммитом, но ничто этого не проверяет — гейт держится только на дисциплине агента. Найдено 2026-07-28 контр-анализом T-106. **Прежде записанный фикс («добавить `"prepare": "lefthook install"`») ОШИБОЧЕН — проверено 2026-07-30:** (1) `frontend/lefthook.yml` запускает `npm run lint` с рабочим каталогом = корень репозитория, где нет `package.json`, то есть хук падал бы на каждом коммите; (2) конфиг лежит не в корне, и lefthook, запущенный из корня, его не найдёт; (3) хуки живут в **общем** `.git/hooks` (шарятся всеми worktree), а сгенерированный скрипт зашивает абсолютный путь того worktree, из которого ставился, — установленный сейчас `prepare-commit-msg` указывает на временный worktree и уже сломан («Can't find lefthook in PATH» на каждом коммите); (4) фильтров по файлам в конфиге нет, поэтому линт и сборка запускались бы даже на коммитах, меняющих только `.ai/*.md`. Настоящий фикс — перенести конфиг и зависимость lefthook в корень репозитория, добавить `glob`-фильтры и запускать команды через `npm --prefix frontend`; ставить хуки только из основной рабочей копии, не из worktree | **ЗАКРЫТ РЕШЕНИЕМ «НЕ ДЕЛАЕМ» 2026-08-03** (T-306). A2 показал, что и этот план дороже, чем записано: сгенерированный хук ищет `$(git rev-parse --show-toplevel)/node_modules`, то есть «перенести в корень» означает завести **корневой `package.json` и корневые `node_modules`** в репозитории, намеренно разделённом на `backend/` и `frontend/` — структурное изменение (**K-37**) ради дублирования того, что `frontend-ci.yml` уже делает на каждом PR теми же командами (lint + test + build). Цена хуков измерена, а не предположена: `.git/hooks` общий для всех worktree, установка зашивает абсолютный путь той копии, откуда ставили, — осиротевший `prepare-commit-msg` (2513 Б, 29.07.2026) указывал на **удалённый** worktree `worktask-live-check-healthcheck-1c605c` и печатал ошибку на каждом коммите в каждой копии (замерено вживую: 3 коммита — 3 ошибки). Сделано вместо фикса: lefthook удалён из проекта целиком (`frontend/lefthook.yml`, зависимость, запись в `.gitignore`, которая существовала только из-за его postinstall), осиротевший хук удалён из `.git/hooks`. **K-06** остаётся ручным и честно помечен таковым в `PROJECT_RULES §Механизмы, которых нет`; машинный гейт тех же проверок — **K-08** |
| TD-035 | Правило гигиены чекпоинта не может сработать: `CLAUDE.md:41-42` предписывает при превышении ~200 КБ вынести в `.ai/checkpoint-archive/` журнал **старше полугода**. Измерено 2026-08-03: `PROJECT_CHECKPOINT.md` — **231 КБ** (порог перейдён), самая старая запись — **2026-06-22** (~6 недель), то есть критерий отбирает **ноль** записей; каталога `.ai/checkpoint-archive/` не существует (`Test-Path` → False) | Порог и критерий отбора формулировались независимо друг от друга: порог — от размера файла, критерий — от возраста записей, и на молодом проекте они не пересекаются | LOW: на работу не влияет — стартовое чтение и так ограничено первыми ~200–300 строками, но механизма усечения у файла фактически нет | **УСТРАНЁН 2026-08-03.** Критерий отбора переписан с возраста на объём — теперь правило исполнимо, и оно исполнено: журнал 2026-06-21…2026-07-06 (55 записей, 1044 строки) вынесен в `.ai/checkpoint-archive/2026-06-21--07-06.md`, основной файл ужался с 2883 до 1772 строк и **178 КБ** — ниже порога. **A1 нашёл дефект крупнее исходного:** в хвосте файла лежал блок «состояния» из фазы 0 (строки 2807–2883) — `Текущий прогресс`, `Активная задача`, `Оставшиеся задачи`, `Риски`, `Технический долг`, `Состояние репозитория`, `Следующие шаги`. Он утверждал «фаза 0 завершена, следующая — 1», «активная задача T-101 (Docker недоступен)», «backend tests 37/37», «TD-001…TD-004», содержал адрес чужой БД `91.211.249.37:32505` и **противоречил сам себе**: два разных заголовка «Последняя завершённая» (T-100 и Фаза 1.5). Блок удалён, а запрет держать состояние в чекпоинте закреплён в `CLAUDE.md` — это ровно «три файла, описывающие одно состояние в трёх формах» (`RULES_DELIVERY_AUDIT §…`) |
| TD-036 | `ValidateTask` — **мёртвый валидатор**: класс помечен `@Component`, метод `validateTask(TaskModel)` публичен, но во всём `backend/src` его не вызывает **никто** (`grep ValidateTask` даёт только сам класс и несвязанный `TaskValidator`). Внутри — правило `validateProjectId`: задача со спринтом допустима, только если `project.getStatus() == ACTIVE` (`ValidateTask:42-46`). Новый проект при этом создаётся как **`DRAFT`** (`Project.java:64`), а перевод в `ACTIVE` требует отдельного `POST /projects/{id}/start` | Валидатор писался под ранний замысел «проект нужно запустить», который в продукт не вошёл; отключён отсоединением, а не удалением | LOW сейчас, но с ловушкой: код **выглядит** действующим правилом. Подключение его «обратно» сломало бы создание задач в каждом новом проекте — то есть это мина для будущего рефакторинга (**K-32**: мёртвый код). Найдено 2026-08-03 при разборе G-10. **Задача не заведена:** удаление трогает backend, а его тесты не идут из рабочей копии (кириллица в пути) — нужен прогон в ASCII-копии, это отдельная работа, а не попутная правка | OPEN |
| TD-037 | Архивация проекта недостижима из интерфейса: `PUT /projects/{id}/archive` существует (T-153), функция `archiveProject` заведена в клиенте (`shared/api/endpoint/projectsApi.ts:114`), но **ни одного вызова** во фронтенде нет. Вдобавок `ShortProjectDataDto` несёт только `id` и `name` — **статуса проекта в списке нет вообще**, поэтому переключатель не смог бы отличить архивный проект, даже если бы архивация появилась | Возможность заведена на бэкенде авансом; UI под неё не делался | LOW: недоступная функциональность (**K-32**), обходного пути нет, но и потребность не заявлена — у владельца два проекта. **Этим же снимается G-10 аудита T-500** («архивный проект неотличим в переключателе»): состояние недостижимо, потому что заархивировать проект нельзя. Вопрос владельцу из `MULTIPROJECT_AUDIT §7` переформулируется: не «показывать ли архивные», а «нужна ли архивация проектов вообще». Если да — сначала аддитивно добавить `status` в `ShortProjectDataDto`. Найдено 2026-08-03 | OPEN |
| TD-039 | `AGENTS.md` выгружается, но **не доезжает до репозитория сам**: T-514 отдаёт готовое содержимое (`GET /rule-sets/project/{id}/agents-md`), а положить файл в репозиторий и закоммитить обязан человек или агент. Критерий задачи формулировался как «файл генерируется **в репозиторий** проекта», и эта часть не выполнена | Интеграция с GitHub в проекте read-only и без токена (`GitHubDevPanelService`: «репозиторий публичный, REST API вызывается БЕЗ токена — секреты не нужны»), а запись требует токена с правом `contents:write`. Секреты заводит только владелец (**K-33**), агент их не запрашивает | LOW: обходной путь есть и он рабочий — скачать или скопировать файл из настроек проекта. Риск в другом: без автоматической выгрузки файл в репозитории **стареет молча**, а `AGENTS.md` помечен как сгенерированный, то есть выглядит свежим. Фикс, когда владелец заведёт токен: шаг выгрузки в пайплайне проекта либо commit через GitHub API; до тех пор в самом файле стоит отметка времени генерации — единственное, что позволяет заметить устаревание. Найдено 2026-08-05 при T-514 | OPEN |
| TD-038 | `.ai/RULES_TAXONOMY.md` §1 («Итог в цифрах») разошёлся с реальностью и **сам с собой**. Измерено 2026-08-05 при A0 задачи T-513: §1 обещает «ядро 36 · паки 11 · профиль 14», тогда как в `.ai/PROJECT_RULES.md` **46 правил ядра и 16 правил паков**, а §4 самого же документа перечисляет 13 правил паков — то есть §1 противоречит §4 внутри одного файла. «Профиль (14)» не существует ни в каком виде: профиль — это контракт возможностей (команды, стек), у него нет ID и правилами эти строки не являются | Документ написан 2026-07-28 как снимок «до» и с тех пор не пересматривался; ядро выросло на K-43…K-46, паки уточнены T-107, а числа остались от снимка | LOW: разбор помечен «снимок, а не рабочий набор», и рабочим реестром служит `PROJECT_RULES.md`. Но числа читаются как актуальные — на них уже опиралось описание T-513, и его пришлось переписывать по факту (**K-46**). Фикс: либо пересчитать §1 и §4, либо явно пометить документ датой-снимком в шапке каждого раздела с числами | **УСТРАНЁН 2026-08-07.** Взяты **обе** половины записанного фикса, потому что каждая по отдельности недостаточна — это показал A1. **Дефект оказался шире описания: числа не «устарели», а не сходились со своими же таблицами с первого дня.** Измерено: §3 перечисляет **42** правила (K-01…K-42, пропусков в нумерации **нет**, тогда как приписка ссылалась на «пропуски после слияния дублей»), §4 — **13**, §5 — **ни одного** правила с ID; не сходилась и арифметика самой таблицы: 8 + 36 + 11 + 14 = **69** при обещанных «~57 после снятия дублей». То есть простая пометка датой закрепила бы неверный счёт, а простой пересчёт «на сегодня» сделал бы документ **вторым реестром** — он разошёлся бы снова (за 8 дней после снимка ядро выросло с 42 до 46), а машинного гейта под него нет (**TD-040**). Сделано: снимок приведён в соответствие со своими таблицами (42 / 13 / 0 / 11 из 42), следствие «две трети ядра не нужны» исправлено на «три четверти», размеры файлов помечены датой снимка, а в шапку добавлена оговорка, что документ **намеренно не поддерживается** и источник действующих правил — только `.ai/PROJECT_RULES.md`. Актуальные числа приведены один раз как **датированное измерение** (на 2026-08-07: ядро 46, паки 16, всего 62), а не как поддерживаемое значение |
| TD-040 | Гейт, которым **ADR-023 оправдывает дубль правил**, не запускается на изменении, которое его ломает. `ReferenceCatalogSyncTest:31` читает `Path.of("..", ".ai", "PROJECT_RULES.md")` и валит сборку, если сгенерированный каталог `workhelper-reference.json` разошёлся с реестром. Но `backend-ci.yml:8,11` фильтрует пути `backend/**`, `docker-compose.vds.yml`, `.github/workflows/deploy.yml` — **`.ai/PROJECT_RULES.md` в списке нет**. PR, меняющий только реестр правил, не запускает ни один workflow (это же записано в граблях окружения: правки в `.ai/*.md` и `CLAUDE.md` не запускают ничего) | Фильтр путей писался под «код backend», а T-513 позже сделал файл вне `backend/` входом теста. Тот же довод в этом же файле уже применён к `docker-compose.vds.yml` и `deploy.yml` (комментарий `backend-ci.yml:3-5`: «перечислены наравне с кодом, их согласованность проверяет `DeployGateContractTest`»), но к реестру правил его не применили | MEDIUM: **ADR-023 разрешает дубль эталонного каталога ровно при условии, что расхождение красит сборку** — а на реестро-only PR оно её не красит. Два следствия: (1) расхождение доезжает до `main` незамеченным и всплывает на следующем backend-PR, окрашивая его по посторонней причине (класс TD-029: гейт, который не детектирует); (2) до этого момента пользователи импортируют **устаревший** эталонный набор «Ядро WorkHelper», и ничто об этом не сообщает (**W-06**). Фикс однострочный: добавить `.ai/PROJECT_RULES.md` в оба списка `paths`. Найдено 2026-08-07 контр-анализом (A2) задачи TD-038 | **УСТРАНЁН 2026-08-07** (T-311). A1 очертил границу точно: файлы вне `backend/` читают **ровно два** теста — `DeployGateContractTest` (`docker-compose.vds.yml`, `deploy.yml` — оба уже были в фильтре) и `ReferenceCatalogSyncTest` (реестр правил — единственный пробел). **A2 нашёл, что дефект шире записанного:** `PROJECT_RULES §Механизмы, которых нет` объявлял **K-08** машинным гейтом взамен удалённых хуков со словами «CI на каждом PR», тогда как workflow фильтруются по путям и PR, не задевший отслеживаемых путей, проходит без единой проверки — то же **K-40**, что и сам TD-040. Сделано: в `paths` добавлены `.ai/PROJECT_RULES.md` и **сам workflow** (по образцу `mcp-ci.yml` — иначе опечатка в фильтре отключила бы проверки молча и не была бы замечена собственным прогоном); в шапке `backend-ci.yml` записан принцип состава фильтра вместе с тем, какой тест читает какой файл; в реестре правил оговорена фактическая область действия K-08. **Фикс самопроверяемый:** PR трогает `.ai/PROJECT_RULES.md`, поэтому исправленный фильтр обязан запустить backend CI на нём самом — это и есть доказательство, а не заявление |
| TD-022 | Неизвестный путь/метод API отвечает `401 Unauthorized` вместо `404`/`405`: Spring форвардит на `/error`, который закрыт общим правилом `anyRequest().authenticated()` | Историческая конфигурация SecurityConfig | LOW: диагностика вводит в заблуждение — «эндпоинта нет» и «нет прав» неотличимы (при аудите ТП-212 это пришлось разделять сравнением с заведомо несуществующим путём). Фикс: разрешить `/error` в SecurityConfig | **УСТРАНЁН 2026-07-27**: `/error` добавлен в `permitAll` (`ERROR_URLS`) — несуществующий путь снова отвечает 404, неподдерживаемый метод 405. Тело ответа формирует общий обработчик, лишнего не раскрывает. **Уточнение 2026-08-05 (измерено на проде):** это верно только для **аутентифицированных** запросов. Без токена любой путь под `/work-task/` отдаёт **401** — проверено: `/work-task/api/v1/nope-does-not-exist`, `/work-task/api/v2/health` и `/work-task/nope` дают 401, тогда как `/health` даёт 200. Причина не регрессия, а порядок: Spring Security отвергает запрос в фильтре **раньше**, чем диспетчер обнаружит отсутствие маппинга, поэтому forward на `/error` не происходит вовсе. Практически это не мешает — вызовы из UI идут с токеном, и там 404/405 различимы (**W-02**), — но диагностировать «эндпоинт есть или нет» безымянной пробой снаружи нельзя, и прежняя безусловная формулировка это скрывала (**K-40**) |

---

## Устранённый технический долг

| ID | Описание | Дата устранения | PR/Commit |
|----|----------|-----------------|-----------|
| BUG-030 | Фронт знал только `TASK`/`BUG` и `LOW`/`MEDIUM`/`HIGH`, а маппер БРОСАЛ на прочих значениях бэкенда (`RESEARCH`, `STORY`, `BLOCKER`) → 5 задач-исследований в бэклоге роняли запрос `sprint-list`, «Список задач» показывал одну секцию «Завершённые» без единой ошибки (CRITICAL) | 2026-07-28 | `claude/workhelper-missing-tasks-backlog-1ac14c`, post-mortem 2026-07-28 |
| BUG-024 | Рассинхрон статус↔спринт: задача, созданная в активный спринт, получала скрытый BACKLOG-статус и «исчезала» из UI; перенос между спринтами/удаление колонки/удаление-архивация-завершение спринта не синхронизировали статус (CRITICAL) | 2026-07-03 | `fix/backlog-status-sprint-consistency`, TaskPlacementService |
| BUG-025 | Задачи без исполнителя не попадали на доску (`/tasks/tasks-in-project` отбрасывал их) (HIGH) | 2026-07-03 | `fix/backlog-status-sprint-consistency` |
| BUG-026 | `TaskModel.setAssignee` — NPE при назначении исполнителя задаче, у которой его не было (HIGH) | 2026-07-03 | `fix/backlog-status-sprint-consistency` |
| BUG-027 | Изображения-вложения не отображались и не скачивались из UI: `<img src>`/`<a href>` без JWT → 401 (HIGH) | 2026-07-03 | `fix/backlog-status-sprint-consistency` |
| BUG-028 | Календарь: `GET /users` доступен только ADMIN/PROJECT_OWNER → 403 у участника проекта, пустой список участников встречи (MEDIUM) | 2026-07-03 | `fix/backlog-status-sprint-consistency` |
| BUG-029 | Раздел Backlog показывал задачи дефолтного спринта с «доскными» статусами → дублирование доска/Backlog (MEDIUM) | 2026-07-03 | `fix/backlog-status-sprint-consistency` |
| BUG-030 | Мёртвый клиентский вызов `GET /tasks` (эндпоинт не существует) (LOW) | 2026-07-03 | `fix/backlog-status-sprint-consistency` |
| BUG-031 | `PUT /tasks/update-sprint` → 404: фронтенд шлёт `sprintId`, бэкенд ждал `targetSprintId` — перенос задач в спринт и перенос незавершённых при закрытии спринта не работали вовсе (CRITICAL) | 2026-07-03 | UpdateTasksSprintRequestDto |
| BUG-032 | `GET /projects/last` возвращал голую строку вместо `IdResponse {id}` — «активным» становился случайный первый проект из неотсортированного списка; Backlog фильтровался по статусам чужого проекта (HIGH) | 2026-07-03 | IdResponse + сортировка for-user |
| BUG-033 | `archiveDoneTasks` сравнивал код колонки с "DONE", а колонки создаются как "Done" — Done-задачи не архивировались при завершении спринта (HIGH) | 2026-07-03 | последняя видимая колонка (max priority) |
| BUG-034 | `FinishSprintModal`: сравнение статусов по ссылке (`task.status !== resolveStatus`) — все задачи всегда «незавершённые»; `resolveStatus` мог выбрать скрытую колонку Canceled (HIGH) | 2026-07-03 | сравнение по id + только видимые колонки |
| BUG-035 | Упоминания `@username` не работали для 2-символьных usernames (например `vt` из префикса email): regex требовал 3+ (MEDIUM) | 2026-07-03 | политика и regex согласованы на 2-32 |
| BUG-011 | `sonner` установлен но `<Toaster>` не подключён → ни одно уведомление не отображалось (HIGH) | 2026-06-22 | `fix/quality-audit-ux-defects` |
| BUG-012 | `apiMiddleware.ts`: при неудачном refresh-токена только `console.error` без редиректа → пользователь застревал с 401 (HIGH) | 2026-06-22 | `fix/quality-audit-ux-defects` |
| BUG-013 | `CreateTaskModal`: non-null assertion `find()!.id` → крэш при отсутствии default-спринта (HIGH) | 2026-06-22 | `fix/quality-audit-ux-defects` |
| BUG-014 | Все мутации без `onError` → ошибки API молча игнорировались (MEDIUM) | 2026-06-22 | `fix/quality-audit-ux-defects` |
| BUG-015 | `EditTaskPage` catch-блок только `console.log` → ошибка сохранения незаметна (MEDIUM) | 2026-06-22 | `fix/quality-audit-ux-defects` |
| BUG-016 | `CreateTaskPage` без try/catch в submit → unhandled promise rejection (MEDIUM) | 2026-06-22 | `fix/quality-audit-ux-defects` |
| BUG-017 | `ViewSprintButton` — заглушка `console.log` → кнопка "просмотреть спринт" не работала (MEDIUM) | 2026-06-22 | `fix/quality-audit-ux-defects` |
| BUG-018 | `FinishSprintModal` без защиты от двойного сабмита → многократный вызов API (MEDIUM) | 2026-06-22 | `fix/quality-audit-ux-defects` |
| BUG-019 | `FinishSprintModal` вызывал `updateTasksSprint` при пустом списке незакрытых задач (LOW) | 2026-06-22 | `fix/quality-audit-ux-defects` |
| BUG-020 | `Sprint` без empty state → при 0 задачах пустое пространство без объяснений (LOW) | 2026-06-22 | `fix/quality-audit-ux-defects` |
| BUG-021 | `Sprint` expand-кнопка скрывалась при активном фильтре → нельзя свернуть/развернуть (LOW) | 2026-06-22 | `fix/quality-audit-ux-defects` |
| BUG-022 | `CompactTaskForm` MenuItem спринтов без `key` prop → React предупреждения + непредсказуемый diff (LOW) | 2026-06-22 | `fix/quality-audit-ux-defects` |
| BUG-023 | `useRegister` опечатка "Пароль должлен" → "Пароль должен" (LOW) | 2026-06-22 | `fix/quality-audit-ux-defects` |
| BUG-001 | `task_status_id_seq` рассинхрон после seed → невозможно создать проект (CRITICAL) | 2026-06-21 | `20250630-fix-task-status-sequence.xml` |
| BUG-002 | `users_projects_id_seq` тот же рассинхрон → невозможно создать проект (CRITICAL) | 2026-06-21 | `20250630-fix-task-status-sequence.xml` |
| BUG-003 | `SprintsRepository.hasActiveSprint` — сломанный native SQL → 500 при активации спринта (HIGH) | 2026-06-21 | SprintsRepository.java |
| BUG-004 | Таблица `comment` без колонки тела → 500 при создании комментария (HIGH) | 2026-06-21 | `20250701-add-comment-body-column.xml` |
| BUG-005 | `TaskService.getProjectTaskByUserGuid` передавал userId вместо lastProjectId → 404 (HIGH) | 2026-06-21 | TaskService.java |
| BUG-006 | Задача без `sprintId` падала с 404 вместо фоллбэка в Backlog (MEDIUM) | 2026-06-21 | SprintsService.resolveSprintForTask |
| BUG-007 | `createProject` возвращал пустые statuses/users (граф не подгружен) (MEDIUM) | 2026-06-21 | ProjectsService (EntityManager.refresh) |
| BUG-008 | `update-status` с null статусом → 500 NPE вместо 400 (LOW) | 2026-06-21 | UpdateStatusRequestDTO @NotNull |
| BUG-009 | `User.extendedPermissions` — `@OneToMany` без `mappedBy` → JPA искал несуществующую join-таблицу `users_extended_permissions` → 500 на `/users/profile`, ломая логин в UI (CRITICAL) | 2026-06-21 | User.java `mappedBy = "user"` |
| BUG-010 | CORS: backend разрешал только `localhost:5173`, а frontend (vite) работает на `localhost:3000` → 403 «Invalid CORS request» из браузера, логин в UI падал с «Неверное имя/пароль» (CRITICAL) | 2026-06-21 | application-local.yml + SecurityConfig (origins через запятую) |

---

## Правила ведения реестра

- Добавлять новый долг немедленно при обнаружении
- При устранении — переносить в секцию "Устранённый"
- Приоритет устранения: HIGH > MEDIUM > LOW
- CRITICAL блокирует релиз
