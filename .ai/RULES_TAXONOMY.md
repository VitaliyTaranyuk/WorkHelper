# Разбор текущего набора правил по уровням переносимости

> Дата: 2026-07-28. Источники разбора: `CLAUDE.md` (26.7 КБ) и `.ai/PROJECT_RULES.md` (26.1 КБ),
> плюс `.ai/SKELETON_FIRST_STANDARD.md` (4.2 КБ) и `.ai/POSTMORTEM_TEMPLATE.md` (3.7 КБ).
> Цель — получить переносимое ядро правил и увидеть дубли.
> Ничего не удалено и не изменено: это разбор, а не рефакторинг.

> **ЭТО СНИМОК НА 2026-07-28, А НЕ РАБОЧИЙ НАБОР ПРАВИЛ.** Все числа, размеры файлов и
> перечни ниже описывают состояние на дату разбора и **намеренно не поддерживаются в
> актуальном виде**. Единственный источник действующих правил — реестр
> `.ai/PROJECT_RULES.md`; сверяться и ссылаться нужно на него.
>
> Оговорка добавлена 2026-08-07 при устранении **TD-038**: числа этого документа читались
> как актуальные, и на них уже опиралось описание T-513 — его пришлось переписывать по факту
> (**K-46**). Тогда же исправлена арифметика самого снимка: часть итогов не сходилась с
> собственными таблицами документа с первого дня. Что именно было неверно — в конце §1.

## Обозначения

**Уровень:** `0` — становится функцией платформы (перестаёт быть текстом) · `ЯДРО` — переносится
в любой проект · `ПАК` — переносится в проекты того же класса · `ПРОФИЛЬ` — принадлежит
конкретному проекту.

**Тип:** принцип (как думать) · гейт (что истинно, чтобы двигаться дальше) · процедура
(последовательность) · запрет.

**Сила:** MUST / SHOULD.

**Проверка:** авто (машиной) · полуавто · ручная.

**Когда:** триггер загрузки. `всегда` — попадает в каждую задачу; остальные — по условию.

---

## 1. Итог в цифрах (снимок 2026-07-28)

| Показатель | Значение на 2026-07-28 | Откуда считается |
|---|---|---|
| Объём правил, загружаемых на старте сессии | ~190 КБ (`CLAUDE.md` 26.7 + `PROJECT_RULES.md` 26.1 + `PROJECT_CHECKPOINT.md` 128 + `TASK_REGISTRY.md` 10.7) | размеры файлов на дату снимка |
| Из них дублирующегося текста | два файла по ~26 КБ пересекаются примерно на 70–80% содержания | §7 |
| Уникальных формулировок правил | ~65 — оценка, сделанная **до** присвоения ID | не сверяется с таблицами ниже |
| После снятия дублей | ~57 — та же оценка | не сверяется с таблицами ниже |
| Уходит в функции платформы (уровень 0) | 8 блоков | §2 |
| Остаётся в ядре | **42 правила** (K-01…K-42) | §3 |
| Уходит в стековые паки | **13 правил** в пяти паках | §4 |
| Уходит в профиль проекта | **0 правил** — профиль это контракт возможностей, 18 строк без ID | §5 |
| Кандидаты на удаление (балласт) | 6 блоков | §6 |
| Правил ядра с триггером «всегда» | **11 из 42** | §3 |

Ключевое следствие последней строки: **три четверти ядра не нужны в большинстве задач.**
Сейчас грузится всё и всегда.

> **Что здесь было неверно и исправлено 2026-08-07 (TD-038).** Прежняя таблица обещала
> «ядро 36 · паки 11 · профиль 14». Ни одно из трёх не сходилось с собственными таблицами
> документа **уже в день написания**: §3 перечисляет 42 правила, §4 — 13, §5 не содержит ни
> одного правила с ID. Не сходилась и арифметика самой таблицы: 8 + 36 + 11 + 14 = 69 при
> обещанных «~57 после снятия дублей». То есть это была не только потеря актуальности, но и
> ошибка счёта с первого дня — поэтому пометки датой было бы недостаточно.
>
> Две строки с «~» оставлены как есть: это оценки, сделанные до того, как правила получили
> ID, и свести их с таблицами задним числом нельзя. Они помечены оценками, чтобы их не
> складывали с точными числами.
>
> **Актуальные числа берутся только из реестра** `.ai/PROJECT_RULES.md` — здесь они не
> поддерживаются. Для масштаба: на 2026-08-07 в реестре 46 правил ядра и 16 паковых, всего
> 62. Это датированное измерение, а не значение, которое будут обновлять в этом документе.

---

## 2. Уровень 0 — перестаёт быть правилом, становится функцией платформы

Эти пункты сейчас написаны текстом, потому что у агента нет системы, которая бы их держала.
Как только проект живёт в WorkTask, текст не нужен — работает продукт.

| Что сейчас | Во что превращается |
|---|---|
| `CONTEXT LOADING ORDER` — порядок чтения файлов при старте | Контекст-бандл под задачу |
| `TOKEN ECONOMY` — «не цитировать правила, ссылаться» | Следствие бандла; правило исчезает |
| `TASK MANAGEMENT` — реестр задач в файле, статусы `NOT_STARTED…` | Задачи и статусы доски в WorkTask |
| `PROJECT CHECKPOINT` — файл прогресса | Состояние проекта в WorkTask |
| `TECHNICAL DEBT` — реестр в `.ai/TECH_DEBT.md` | Раздел техдолга проекта |
| `AUTONOMOUS EXECUTION MODE` — что можно без подтверждения | Настройка проекта «уровень автономии» |
| `BRANCH PROTECTION` — включить защиту `main` | Действие при подключении репозитория |
| `PROJECT STRUCTURE` — описание дерева каталогов | Читается из репозитория; в профиле остаются только команды |

**Экономия:** ~8 блоков текста и, что важнее, `PROJECT_CHECKPOINT.md` (128 КБ на дату
снимка) перестаёт читаться целиком на каждом старте.

---

## 3. Уровень ЯДРО — переносится в любой новый проект

### 3.1 Процесс и Git

| ID | Правило | Тип | Сила | Когда | Проверка |
|---|---|---|---|---|---|
| K-01 | Одна задача = одна ветка | процедура | MUST | всегда | полуавто |
| K-02 | Запрещено работать напрямую в `main`; merge только через PR | запрет | MUST | всегда | авто |
| K-03 | Ветка именуется по типу работы (`feature/`, `fix/`, `refactor/`, `test/`, `docs/`) | процедура | SHOULD | старт задачи | авто |
| K-04 | Conventional Commits | процедура | MUST | коммит | авто |
| K-05 | Коммит атомарный, логически завершённый, протестированный | принцип | SHOULD | коммит | ручная |
| K-06 | Quality gate перед коммитом: формат → линт → статанализ → тесты → сборка | гейт | MUST | коммит | авто |
| K-07 | При падении любой проверки коммит запрещён до устранения причины | запрет | MUST | коммит | авто |
| K-08 | Каждый PR проходит те же проверки в CI; красный CI блокирует merge | гейт | MUST | PR | авто |
| K-09 | Definition of Done: код, тесты, документация, коммит, PR, нет блокирующих дефектов | гейт | MUST | завершение | полуавто |

### 3.2 Принятие решений и работа с фактами

| ID | Правило | Тип | Сила | Когда | Проверка |
|---|---|---|---|---|---|
| K-10 | Запрещено выдумывать API, библиотеки, результаты тестов и исследований | запрет | MUST | всегда | ручная |
| K-11 | Запрещено сообщать о выполнении действий, которые не выполнялись | запрет | MUST | всегда | ручная |
| K-12 | Приоритет источников истины: репозиторий → issues/PR → состояние проекта → код → внешние источники | принцип | MUST | всегда | ручная |
| K-13 | Предположение обозначается как предположение | принцип | MUST | всегда | ручная |
| K-14 | Не задавать вопросы, ответ на которые есть в коде, истории или документации | принцип | SHOULD | всегда | ручная |
| K-15 | При неполных требованиях выбирать наиболее консервативный вариант, согласованный с существующей архитектурой | принцип | SHOULD | неясные требования | ручная |
| K-16 | Приоритет качеств при компромиссах: ценность → надёжность → безопасность → поддерживаемость → производительность → масштабируемость → скорость | принцип | SHOULD | архитектурное решение | ручная |
| K-17 | Порядок поставки: критичное → основной сценарий → расширенные → дополнительные → косметика | принцип | SHOULD | планирование | ручная |

### 3.3 Перед разработкой

| ID | Правило | Тип | Сила | Когда | Проверка |
|---|---|---|---|---|---|
| K-18 | Для значимого функционала — исследование: конкуренты, практики, боли, минимум с максимальной ценностью | процедура | SHOULD | значимый функционал | ручная |
| K-19 | Не реализовывать функционал без обоснования пользы | запрет | SHOULD | значимый функционал | ручная |
| K-20 | Build-vs-buy: проверенное OSS → адаптация → своя реализация | принцип | SHOULD | новая подсистема | ручная |
| K-21 | Ветинг зависимости: поддержка, документация, лицензия, CVE, популярность | процедура | MUST | новая зависимость | полуавто |
| K-22 | Перед новым кодом изучить существующий и проверить возможность переиспользования и расширения | процедура | MUST | всегда | ручная |
| K-23 | Не трогать модули, не связанные с задачей | запрет | MUST | всегда | полуавто |

### 3.4 Качество и тестирование

| ID | Правило | Тип | Сила | Когда | Проверка |
|---|---|---|---|---|---|
| K-24 | Тесты пишутся до или одновременно с кодом | процедура | SHOULD | реализация | ручная |
| K-25 | Критическая бизнес-логика имеет тестовое покрытие (порог — параметр проекта) | гейт | MUST | реализация | авто |
| K-26 | Не останавливаться на первой ошибке: найти первопричину, исправить, перезапустить затронутые и зависимые тесты | процедура | MUST | падение тестов | ручная |
| K-27 | Ошибка исправляется через первопричину: причина → фикс → тест → проверка соседних компонентов | процедура | MUST | багфикс | ручная |
| K-28 | Сбежавший в прод баг сначала воспроизводится падающим тестом; тест остаётся навсегда | процедура | MUST | прод-баг | полуавто |
| K-29 | Уровни дефектов (CRITICAL/HIGH/MEDIUM/LOW) с критериями; CRITICAL и HIGH устраняются до новых функций | процедура | MUST | багфикс | ручная |
| K-30 | Самопроверка до завершения: архитектура, код, безопасность, производительность, тесты | процедура | MUST | завершение | ручная |
| K-31 | Перед завершением проверить затронутые сценарии: навигация, переходы состояний, persistence, права, loading/error/empty, границы | процедура | MUST | завершение | ручная |
| K-32 | Признаки незавершённости: мёртвые кнопки, разорванные сценарии, несогласованные состояния, необработанные ошибки, отсутствующая валидация или persistence, падающие тесты | гейт | MUST | завершение | ручная |

### 3.5 Безопасность

| ID | Правило | Тип | Сила | Когда | Проверка |
|---|---|---|---|---|---|
| K-33 | Секреты никогда не попадают в репозиторий, код или текст команд; только окружение | запрет | MUST | всегда | авто |
| K-34 | Наружу — понятное сообщение; SQL, имена constraint и stacktrace только в логи | запрет | MUST | обработка ошибок | полуавто |
| K-35 | Проверять права доступа, валидацию ввода на всех уровнях, уязвимости зависимостей | процедура | MUST | API/доступ | полуавто |
| K-36 | Персональные данные: минимизировать сбор, не логировать в открытом виде, ограничивать по ролям, пароли — только хэш | запрет | MUST | проект хранит ПДн | полуавто |

### 3.6 Архитектура, документация, откат

| ID | Правило | Тип | Сила | Когда | Проверка |
|---|---|---|---|---|---|
| K-37 | Архитектурный стиль, структура модулей и основные зависимости не меняются без технического обоснования и документирования | запрет | MUST | архитектурное изменение | ручная |
| K-38 | Существующий паттерн проекта важнее нового: consistency over novelty | принцип | MUST | всегда | ручная |
| K-39 | Каждое значимое решение документируется: проблема → альтернативы → выбор → причины → последствия | процедура | MUST | значимое решение | ручная |
| K-40 | Документация соответствует реальному состоянию проекта | гейт | MUST | завершение | ручная |
| K-41 | Каждое изменение имеет план отката; для значимых — риски и точки восстановления определены заранее | процедура | MUST | значимое изменение | ручная |
| K-42 | Технический долг фиксируется явно и не скрывается | процедура | MUST | обнаружение | полуавто |

> Итого ядра в снимке: **42 правила** — K-01…K-42, **пропусков в нумерации нет** (пересчитано
> 2026-08-07). Прежняя формулировка «36 правил с учётом пропусков после слияния дублей»
> ссылалась на пропуски, которых в таблицах выше не существует.
> С триггером «всегда» — **11** из 42.
>
> В реестре `.ai/PROJECT_RULES.md` ядро с тех пор доросло до K-46 — этот снимок за ним не
> следует.

---

## 4. Уровень ПАК — стековые наборы (снимок 2026-07-28)

### Пак «клиент-серверное приложение»

| ID | Правило | Источник |
|---|---|---|
| W-01 | Каждый вызываемый фронтендом эндпоинт существует на бэкенде с совпадающим путём, методом и формой DTO; проверяется полный путь end-to-end | урок проекта |
| W-02 | 401/404 на вызов из UI = несуществующий или несогласованный контракт, а не «проблема авторизации» | урок проекта |
| W-03 | Изменения, видимые в приложении, проверяются на развёрнутом окружении с рабочей ссылкой, а не только локальной сборкой | урок проекта |
| W-04 | Бизнес-доступ решается в сервисе; валидатор проверяет только существование и формат | урок проекта |
| W-05 | Проверять граничные роли (только что зарегистрированный пользователь) и неактивные состояния сущностей | урок проекта |

### Пак «фронтенд»

| ID | Правило | Источник |
|---|---|---|
| F-01 | Модалки и панели открываются skeleton-first: мгновенный каркас и известные данные вместо блокирующего спиннера | стандарт ТП-185 |
| F-02 | Тяжёлые блоки грузятся прогрессивно, состояния loading/error/empty честные по блокам | стандарт ТП-185 |
| F-03 | UX-проверка: desktop и mobile, адаптивность, empty/loading/error, доступность, визуальная консистентность | CLAUDE.md |
| F-04 | Удалять дублирующие контролы, мёртвый и вводящий в заблуждение UI | CLAUDE.md |

### Пак «React»

| ID | Правило | Источник |
|---|---|---|
| R-01 | Дерево вне роутов (модалки) оборачивается error boundary; краш блока ≠ белый экран | урок ТП-172 |
| R-02 | Компоненты, переиспользуемые модалками, не используют router-хуки — навигация только прямым вызовом роутера | урок ТП-172, прод-инцидент |

### Пак «тестирование на JVM»

| ID | Правило | Источник |
|---|---|---|
| T-01 | Строгий режим стабов; фикстуры выносятся отдельно, без стабов в setup-методах | PROJECT_RULES |

### Пак «тестирование фронтенда»

| ID | Правило | Источник |
|---|---|---|
| T-02 | Незамеченный `console.error` или uncaught в тесте = красный тест | урок ТП-172 |

> Итого паков в снимке: **13 правил** в пяти наборах — W-01…W-05, F-01…F-04, R-01, R-02,
> T-01, T-02 (пересчитано 2026-08-07; §1 прежде утверждал 11, противореча этому же разделу).
> Уроки W-06…W-08 куплены инцидентами позже и в снимок не входят — они есть в реестре.

---

## 5. Уровень ПРОФИЛЬ — принадлежит конкретному проекту

> **Правил с ID здесь нет ни одного,** и это не пробел, а суть уровня: профиль описывает
> возможности и обстоятельства проекта — команды, стек, домен, — а не нормы. Строка «уходит
> в профиль проекта: 14» в §1 была ошибкой ровно поэтому: считать было нечего. Ниже 18
> строк двух таблиц, ни одна из них правилом не является.

### Контракт возможностей (заменяет правила с командами)

| Операция | Значение в WorkHelper |
|---|---|
| тесты (backend) | `./gradlew test` |
| сборка (backend) | `./gradlew build` |
| запуск (backend) | `./gradlew bootRun` |
| линт (frontend) | `npm run lint` |
| формат+фикс (frontend) | `npm run check` |
| сборка (frontend) | `npm run build` |
| дев-сервер | `npm run dev` |
| БД локально | `docker-compose up -d` в `backend/enviroment/local` |
| генерация типов API | `npm run openapi-generate` |

### Прочее проектное

| Что | Значение |
|---|---|
| Стек | Java 21, Spring Boot 3.4, PostgreSQL + Liquibase, Lombok, MapStruct; React 19, TypeScript, Vite, MUI v7, TanStack Router/Query/Table, Zustand, RHF + Zod, Axios |
| Структура | `backend/`, `frontend/`, `docs/`, `.ai/`, `infra/` |
| Порог покрытия | ≥80% критичной логики (JaCoCo / Vitest c8) |
| CI | `.github/workflows/` |
| Особенность сборки | при новом маршруте регенерировать `routeTree.gen.ts` |
| Деплой | VDS, `docker-compose.vds.yml`, мониторинг GlitchTip |
| Домен | Backlog — отдельная сущность и раздел; доска показывает только активные задачи спринта; календарь с переходом к задаче; уведомления связаны с задачами; настройки с явной точкой входа |
| Эталоны качества | Jira, Linear, ClickUp, Asana, Trello, Monday — как референс, не как объект копирования |
| Документы проекта | `.ai/ARCHITECTURE.md`, `.ai/TECH_DEBT.md`, `.ai/SKELETON_FIRST_STANDARD.md`, `.ai/POSTMORTEM_TEMPLATE.md` |

---

## 6. Балласт — кандидаты на удаление

Критерий: агент знает это и без правила, либо это невозможно проверить, либо это дублирует
то, что видно в репозитории. Каждая строка сейчас оплачивается токенами при каждом запуске.

| Что | Почему кандидат на удаление |
|---|---|
| `CODE QUALITY` — перечисление SOLID, DRY, KISS, YAGNI, Clean Architecture, SoC, DI | Модель это знает. Ценность появляется только если у проекта есть **отклонение** от общепринятого — тогда пишем отклонение, а не список аббревиатур |
| «Композиция вместо наследования», «читаемость вместо преждевременной оптимизации» | То же самое |
| `SYSTEM MODE` — перечисление ролей (Principal Architect, QA, DevOps, Security…) | Ролевое заклинание, непроверяемо; полезность даёт неочевидная информация, а не назначение ролей |
| `FINAL OBJECTIVE` / `PRIMARY OBJECTIVE` | Декларация намерений; не содержит проверяемого требования. Дублирует Definition of Done |
| `PRODUCT STRUCTURE` — описание дерева каталогов | Дублирует репозиторий: агент видит дерево. Оставить только команды (уходят в профиль) |
| Приложение «Согласованность правил» (таблица разрешения кажущихся конфликтов) | Существует только потому, что правила дублируются в двух файлах. При нормализации исчезает вместе с причиной |

---

## 7. Найденные дубли и конфликты

### Дубли между `CLAUDE.md` и `PROJECT_RULES.md`

Почти полностью дублируются: SYSTEM MODE, SOURCE OF TRUTH, RESEARCH FIRST,
ANTI-HALLUCINATION, TASK MANAGEMENT, PROJECT CHECKPOINT, GIT WORKFLOW, COMMIT POLICY,
TESTING, TEST COVERAGE, CI/CD, CODE QUALITY, SECURITY, DOCUMENTATION, ROLLBACK STRATEGY,
DEFINITION OF DONE, FINAL/PRIMARY OBJECTIVE — **17 блоков**.

При этом `CLAUDE.md` загружается автоматически, а `PROJECT_RULES.md` — по инструкции
`CONTEXT LOADING ORDER` из самого же `CLAUDE.md`. То есть корпус оплачивается **дважды
при каждом старте сессии**.

### Реальные противоречия

| # | Противоречие | Где |
|---|---|---|
| 1 | Покрытие: «целевой ориентир 80%, **не блокирующий gate**» против «минимальное покрытие ≥80%, **порог проверяется в сборке/CI**» | `CLAUDE.md` §TEST COVERAGE ↔ `PROJECT_RULES.md` §TESTING POLICY |
| 2 | `main`: «запрещено коммитить напрямую, **единственное исключение — критический hotfix** с уведомлением» против «запрещено работать напрямую в `main`» без исключений | `CLAUDE.md` §GITHUB WORKFLOW ↔ `PROJECT_RULES.md` §GIT WORKFLOW |

Оба противоречия — прямое следствие дублирования: одна и та же норма записана дважды и
разошлась при правках. Это и есть аргумент за «правило формулируется один раз».

> **Статус на 2026-07-28: противоречия закрыты T-106, их причина — T-107.** Таблица выше
> и раздел о дублях сохранены как снимок «до».
>
> **T-106** (PR #201) закрыл оба противоречия плюс третье («состав CI») и четвёртое,
> найденное контр-анализом («Integration Tests объявлены обязательными, но их не
> существует»).
>
> **T-107** (PR #202) устранил причину — дубль. Файлы больше не близнецы, а разделены
> **по триггеру загрузки**: `CLAUDE.md` (15.7 КБ вместо 30.9) — точка входа с
> самодостаточным ядром «всегда» и профилем проекта; `.ai/PROJECT_RULES.md` — реестр из
> 59 правил с ID, уровнем, силой, триггером и способом проверки, грузится по условию
> (59 — столько их было на момент закрытия T-107; актуальное число — по самому реестру).
> Каждая формулировка существует ровно в одном месте; `CLAUDE.md` ссылается по ID и
> текст правил не копирует. Разбор ниже переведён в рабочий набор: колонка «Уровень»
> реализована как разделы реестра (ЯДРО / ПАК / ПРОФИЛЬ), колонка «Когда» — как триггер.

### Формально не конфликт, но требует внимания

`AUTONOMOUS EXECUTION MODE` («не запрашивать подтверждения», «автоматически переходить к
следующей задаче») задаёт уровень автономии, который может расходиться с ожиданиями
пользователя в конкретной сессии. В платформе это должно стать **настройкой проекта**,
а не правилом в тексте.

---

## 8. Что из этого следует

1. **Ядро существует.** 42 правила снимка переносятся в проект на любом стеке без единой
   правки — при условии, что команды вынесены в контракт возможностей.
2. **Дубль — главный источник расхода и расхождений.** Два файла по 26 КБ, 17 общих блоков,
   два разошедшихся правила.
3. **Часть правил не должна быть текстом.** 8 блоков превращаются в функции платформы,
   включая самый дорогой — чекпоинт на 128 КБ (на дату снимка).
4. **Триггеры важнее объёма.** Из 42 правил ядра только 11 нужны в каждой задаче.
   Остальные — по условию.
5. **Уроки проекта — самая ценная часть.** W-01…W-05, R-01, R-02, T-02 куплены реальными
   инцидентами. Именно они дают ту «неочевидную информацию», ради которой правила и нужны.
